version: "3.9"
services:
  mock_api:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ./app.py:/app/app.py:ro
    command: ["sh", "-c", "pip install --no-cache-dir fastapi uvicorn && uvicorn app:app --host 0.0.0.0 --port 9010"]
    ports:
      - "9010:9010"
