version: "3.9"
services:
  mockserver:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ../../tests/mock_api:/app
    command: ["/bin/sh", "-c", "pip install --no-cache-dir fastapi uvicorn && uvicorn app:app --host 0.0.0.0 --port 8008"]
    ports:
      - "8008:8008"
